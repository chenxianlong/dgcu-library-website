# SQLite 数据库必须放在持久化目录中。
LIBRARY_DB_PATH=./data/library.sqlite
LIBRARY_UPLOAD_DIR=./data/uploads

# 用于对客户端地址做不可逆摘要；生产环境请使用至少 32 字节随机值。
AUTH_SECRET=
PUBLIC_ORIGIN=http://localhost:4321

HOST=0.0.0.0
PORT=4321
